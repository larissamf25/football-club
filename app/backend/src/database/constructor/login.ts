class login {
  /* public token: string;

  set token(token) {
    this.token = token;
  }

  create(req: Request, res: Response) {
    const { token } = req;
    set token(token);
    res.status(201).json({ token });
  } */
}

export default login;
